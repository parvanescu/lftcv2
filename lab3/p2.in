decl{
    int a,b,c;
    float x1,x2,delta;
}
prog{
    read(a);read(b);read(c);
    delta = b**2 - 4*a*c;
    if(delta > 0){
        x1 = ( -b + sqrt(delta))/(2*a);
        x2 = ( -b - sqrt(delta))/(2*a);
        write(x1);
        write(x2);
    }else{
        write("complex root extraction is not supported");
    }

}